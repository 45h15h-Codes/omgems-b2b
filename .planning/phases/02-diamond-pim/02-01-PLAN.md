---
phase: 02-diamond-pim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/database/migrations/xxx_create_diamonds_table.php, api/app/Models/Diamond.php, api/app/Http/Controllers/DiamondController.php]
autonomous: true
requirements: [DIAM-01, DIAM-02]
---

<objective>
Implement the core technical database and API for professional diamond listings.

Purpose: Provide the data layer for stone inventory management.
Output: Diamond migration, model, and RESTful API endpoints.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/implementation_plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Diamond Migration</name>
  <action>Run `php api/artisan make:migration create_diamonds_table`. Define full technical schema: shape, carat, color, clarity, cut, polish, symmetry, fluorescence, table_percent, depth_percent, measurements, girdle, culet, lab, certificate_no, certificate_url, price, status, merchant_id.</action>
  <verify>php api/artisan migrate:status | grep diamonds</verify>
  <done>Diamonds table is migrated to MySQL</done>
</task>

<task type="auto">
  <name>Task 2: Implement Diamond Model & Factory</name>
  <action>Create `app/Models/Diamond.php` with mass-assignable specs. Create `database/factories/DiamondFactory.php` to generate realistic technical data for testing.</action>
  <verify>test -f api/app/Models/Diamond.php</verify>
  <done>Model and Factory are ready</done>
</task>

<task type="auto">
  <name>Task 3: Implement DiamondController (CRUD)</name>
  <action>Create `app/Http/Controllers/DiamondController.php`. Implement index, store, show, update, destroy. Ensure index supports pagination and initial filtering by shape/carat.</action>
  <verify>php api/artisan route:list | grep diamonds</verify>
  <done>Diamond CRUD API is functional</done>
</task>

<task type="auto">
  <name>Task 4: Secure Routes & Validation</name>
  <action>Register routes in `api.php`. Use `auth:sanctum` middleware. Create `App\Http\Requests\StoreDiamondRequest` with strict validation for diamond specs (carat must be numeric, shape must be from predefined set).</action>
  <verify>php api/artisan test</verify>
  <done>API is secured and validated</done>
</task>

</tasks>

<verification>
- [ ] api/artisan migrate:status shows diamonds table
- [ ] curl -I http://localhost:8001/api/diamonds returns 200
- [ ] Database contains technical fields (table_percent, etc.)
</verification>

<success_criteria>
- Diamonds table exists with 20+ professional fields.
- Merchants can add stones via authenticated POST request.
- Diamond index supports JSON pagination.
</success_criteria>

<output>
After completion, create .planning/phases/02-diamond-pim/02-01-SUMMARY.md
</output>
