---
phase: 01-foundation-rbac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/.env, api/app/Models/User.php, api/database/seeders/DatabaseSeeder.php]
autonomous: true
requirements: [AUTH-01, AUTH-02, ADMN-04]
---

<objective>
Initialize the Laravel 12 API and set up the RBAC foundation.

Purpose: Provide a secure, scalable backend for the luxury marketplace.
Output: Initialized Laravel app in /api with Sanctum and Role-based access.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Laravel API</name>
  <action>Run `composer create-project laravel/laravel:^12.0 api`. Then install API features with `php artisan install:api`. Configure Sanctum to use cookie-based auth for local development.</action>
  <verify>test -d api/app && php api/artisan --version</verify>
  <done>Laravel 12 is installed in /api</done>
</task>

<task type="auto">
  <name>Task 2: Setup RBAC (Spatie Permission)</name>
  <action>Install Spatie Laravel Permission: `composer require spatie/laravel-permission`. Publish migrations and run them. Update User model to use HasRoles trait.</action>
  <verify>php api/artisan list | grep permission</verify>
  <done>RBAC library installed and User model updated</done>
</task>

<task type="auto">
  <name>Task 3: Configure Environment & Seeders</name>
  <action>Configure api/.env with MySQL settings (default: root, no password, host 127.0.0.1). Create DatabaseSeeder to initialize roles (SuperAdmin, Admin, Merchant) and one SuperAdmin user (admin@omgems.com / pass: admin123).</action>
  <verify>php api/artisan db:seed --force</verify>
  <done>MySQL connected and default admin user seeded</done>
</task>

</tasks>

<verification>
- [ ] api/artisan migrate:status shows all runs
- [ ] api/app/Models/User.php contains HasRoles
- [ ] Database contains roles table and admin user
</verification>

<success_criteria>
- Laravel 12 API is running with Sanctum.
- Roles (SuperAdmin, Admin, Merchant) are defined in DB.
- One SuperAdmin user exists in the database.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-rbac/01-01-SUMMARY.md
</output>
