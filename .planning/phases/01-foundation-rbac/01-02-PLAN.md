---
phase: 01-foundation-rbac
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [web/package.json, web/tailwind.config.js, web/src/app/layout.tsx]
autonomous: true
requirements: [AUTH-01, AUTH-04, UIUX-01]
---

<objective>
Initialize the Next.js 16 frontend and set up the luxury design foundation.

Purpose: Create a cinematic, high-performance UI shell for the marketplace.
Output: Initialized Next.js app in /web with Tailwind 4 and Auth UI.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-rbac/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 16 Frontend</name>
  <action>Run `npx create-next-app@latest web --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*" --use-npm`. Configure `next.config.js` for Static Export (`output: 'export'`).</action>
  <verify>test -f web/package.json && npm --prefix web run build</verify>
  <done>Next.js 16 is installed in /web</done>
</task>

<task type="auto">
  <name>Task 2: Setup Luxury Design System (Tailwind 4 + Lucide)</name>
  <action>Ensure Tailwind CSS 4 is configured. Install `lucide-react` and `framer-motion`. Define luxury design tokens in globals.css (typography: serif for headings, sans for body; colors: high-contrast monochrome with subtle gold/neutral accents).</action>
  <verify>grep "framer-motion" web/package.json</verify>
  <done>Design system foundation is established</done>
</task>

<task type="auto">
  <name>Task 3: Implement Auth Client & Login UI</name>
  <action>Create `lib/api.ts` with Axios instance configured for Sanctum CSRF. implement `app/login/page.tsx` with a premium, minimal aesthetic. logic to handle Laravel login and session persistence.</action>
  <verify>test -f web/app/login/page.tsx</verify>
  <done>Login page and API client are ready</done>
</task>

<task type="auto">
  <name>Task 4: Create Role-Based Layouts</name>
  <action>Create layout skeletons for Super Admin, Admin, and Merchant roles. implement simple middleware/routing logic to direct users based on role after login.</action>
  <verify>test -d web/app/(admin) && test -d web/app/(merchant)</verify>
  <done>Role-based navigation layouts are created</done>
</task>

</tasks>

<verification>
- [ ] npm --prefix web run build succeeds
- [ ] web/app/login/page.tsx renders with luxury styling
- [ ] API client properly sends credentials
</verification>

<success_criteria>
- Next.js 16 is initialized and building as Static Export.
- Luxury design tokens applied to globals.css.
- Login page is functional (UI-wise) and connected to API logic.
- Basic navigation shells for Super Admin, Admin, and Merchant exist.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-rbac/01-02-SUMMARY.md
</output>
