---
phase: 03-premier-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/app/Models/Diamond.php, api/app/Http/Controllers/DiamondController.php]
autonomous: true
requirements: [SRCH-01]
---

<objective>
Implement the backend filtering engine in Laravel to support advanced diamond searches.

Purpose: Provide the data layer for the advanced search tool.
Output: Query scopes and updated controller with filtering logic.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/implementation_plan.md.resolved
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Query Scopes in Diamond Model</name>
  <action>Add `scopeWithFilters` to `Diamond.php`. Implement sub-scopes for `shape` (multi-select), `carat` (min/max), `color`, `clarity`, `cut`, and `price` (min/max).</action>
  <verify>test -f api/app/Models/Diamond.php</verify>
  <done>Model scopes are implemented</done>
</task>

<task type="auto">
  <name>Task 2: Update DiamondController index</name>
  <action>Modify `DiamondController@index` to call the filtering scopes based on input. Add sorting support (price asc/desc, carat desc).</action>
  <verify>Manual API test with ?shape=Round&min_carat=1.0</verify>
  <done>Controller index is updated with filtering</done>
</task>

<task type="auto">
  <name>Task 3: Seed Large Dataset</name>
  <action>Update `DatabaseSeeder.php` or create a dedicated seeder to generate 500+ mock diamonds for testing the search engine performance.</action>
  <verify>php artisan db:seed</verify>
  <done>Database is populated with test data</done>
</task>

</tasks>

<verification>
- [ ] curl "http://localhost:8001/api/diamonds?shape=Round&min_carat=1.0" returns filtered JSON
- [ ] Database contains 500+ records
</verification>

<success_criteria>
- Backend supports complex, multi-parameter diamond filtering.
- API response is paginated and contains correct stone counts.
</success_criteria>

<output>
After completion, create .planning/phases/03-premier-search/03-01-SUMMARY.md
</output>
