---
phase: 03-premier-search
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified: [web/lib/store/useSearchStore.ts, web/app/diamonds/page.tsx]
autonomous: true
requirements: [SRCH-02, SRCH-03]
---

<objective>
Build the frontend search interface and global state management for diamond filtering.

Purpose: Provide a premium, reactive search experience.
Output: Search page, Zustand store, and URL sync logic.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
@C:/Users/ASUS/.gemini/antigravity/skills_old/skills/zustand-store-ts/SKILL.md
@C:/Users/ASUS/.gemini/antigravity/skills_old/skills/nextjs-app-router-patterns/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/implementation_plan.md.resolved
@.planning/phases/03-premier-search/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Diamond Search Store (Zustand)</name>
  <action>Create `web/lib/store/useSearchStore.ts` using the **`zustand-store-ts`** skill. Define `filters` (shape, carat, price, etc.) and `results` (storing current page and total). implement an `updateFilters` action that triggers a new API call.</action>
  <verify>test -f web/lib/store/useSearchStore.ts</verify>
  <done>Zustand store for search is implemented</done>
</task>

<task type="auto">
  <name>Task 2: Build Diamond Search Page (Next.js)</name>
  <action>Create `web/app/diamonds/page.tsx`. Use the **`nextjs-app-router-patterns`** skill to handle client-side search routing. Implement a luxury split-screen layout (Sidebar for filters, Grid for stones).</action>
  <verify>test -f web/app/diamonds/page.tsx</verify>
  <done>Search page layout is ready</done>
</task>

<task type="auto">
  <name>Task 3: Implement URL Query Syncing</name>
  <action>Connect the search store to the Next.js `useSearchParams`. Ensure that changing a filter updates the URL (e.g., `?shape=Round`) so search results are shareable.</action>
  <verify>Updating filters changes the browser URL</verify>
  <done>Filters are synced with the URL</done>
</task>

</tasks>

<verification>
- [ ] User can navigate to /diamonds
- [ ] Filters update URL and fetch new data asynchronously
- [ ] Loading states are smooth
</verification>

<success_criteria>
- Filter state is managed globally by Zustand.
- Search results are reactive and paginated.
- URL accurately reflects the current state.
</success_criteria>

<output>
After completion, create .planning/phases/03-premier-search/03-02-SUMMARY.md
</output>
